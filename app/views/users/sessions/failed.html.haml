%h2 Mmm...

- if @failed == 'no_account' && @provider

  %p
    You don't have an account connected to #{auth_provider_name(@provider)}.

- else

  %p
    Something went wrong
    - if @provider
      with #{auth_provider_name(@provider)}
  %p
    Please try again.
